#include <iostream>
#include <climits>
using namespace std;

//********Arrays in C++***********
// Array is a list of variables of similar types
// eg.1-{1,2,3,4} 2-{'c','d','e','i'} 3-{'d',7,'f',6} here eg. 1 and 2 are the example of
// arrays while examle 3 is not Aray becz not a similar type variables
// Syntax
//  datatypes arrayName[size];
// eg. int array[4] ={10,24,45,67};

int main()
{
    // for already array
    // int array[6] = {1, 2, 3, 4, 5};
    // cout << array[1] << endl;
    // cout << array[2] << endl;
    // cout << array[3] << endl;
    // cout << array[4] << endl;

    // cout << array[3] << endl;

    // want to get size from array then
    // int n;
    // cout << "Enter the size of array " << endl;
    // cin >> n;
    // int array[n];
    // cout << "Enter the values " << endl;
    // for (int i = 0; i < n; i++)
    // {
    //     cin >> array[i];
    // }

    // for (int i = 0; i < n; i++)
    // {
    //     cout << "array no " << i << " " << array[i] << " "<<endl;
    // }

    // int n;
    // cout << "Enter the size of array " << endl;
    // cin >> n;
    // int array[n];
    // for (int i = 0; i < n; i++)
    // {
    //     cin >> array[i];
    // }
    // int maxNo = INT_MIN;
    // int minNo = INT_MAX;
    // for (int i = 0; i < n; i++)
    // {
    //     maxNo = max(maxNo, array[i]);
    //     minNo = min(minNo, array[i]);
    // }
    // cout << "Max no is: " << maxNo << " "
    //      << "Min no is: " << minNo << endl;

    
    return 0;
}