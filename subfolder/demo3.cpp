#include<iostream>
using namespace std;

main(){
 
 // headers files & operators 
 //    Types of header files:
 //    1. System header files: it comes with the compilier 
 //    2. User defined header files: it is written by the programmer 
 // cpprefrence.com search in google to find header files 
  
 // Operators in c++
 //1-Arithmatic operators 

  // int a,b;
  // cin>>a>>b;
  // cout<<"The value of a + b is "<<a+b<<endl; //here 'a+b' is arithmatic operator
  // cout<<"The value of a - b is "<<a-b<<endl; //here 'a-b' is arithmatic operator
  // cout<<"The value of a * b is "<<a*b<<endl; //here 'a*b' is arithmatic operator
  // cout<<"The value of a / b is "<<a/b<<endl; //here 'a/b' is arithmatic operator
  // cout<<"The value of a % b is "<<a%b<<endl; //here 'a%b' is arithmatic operator
  // cout<<"The value of a++ is "<<a++<<endl; 
  // cout<<"The value of a-- is "<<a--<<endl; 
  // cout<<"The value of ++a  is "<<++a<<endl;
  // cout<<"The value of --a  is "<<--a<<endl;
  
    // 2- Assignment operators:
    // int a=4,b=7;
    // char d="d";

   // 3-Comparision operators:
  //  int a,b;
  //  cin>>a>>b; 
  //  cout<<"The value of a != b is "<<(a!=b)<<endl;
  //  cout<<"The value of a < b is "<<(a<b)<<endl;
  //  cout<<"The value of a > b is "<<(a>b)<<endl;
  //  cout<<"The value of a >= b is "<<(a>=b)<<endl;
  //  cout<<"The value of a <= b is "<<(a<=b)<<endl;
  //  cout<<"The value of a == b is "<<(a==b)<<endl;
  
   // Logical operators:
  //  int a,b;
  //  cin>>a>>b;
  //  cout<<"The value of the logical and operators ((a == b) && (a>b)) is "<<((a==b) && (a>b))<<endl;
  //  cout<<"The value of the logical or operators ((a < b) || (a>b)) is "<<((a<b) || (a>b))<<endl;
  //  cout<<"The value of the logical not operators (!(a < b) || (a>b)) is "<<(!(a<b) || (a>b))<<endl;
    


    return 0;
} 